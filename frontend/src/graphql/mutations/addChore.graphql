mutation createChore($args: CreateChoreInput!) {
  createChore(args: $args) {
    id
    title
    description
    createdBy {
      id
      userName
      email
      firstName
      lastName
    }
    dueDate
    createdAt
    isRecurring
    status
  }
}
