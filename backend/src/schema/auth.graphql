type AuthPayload {
  user: User!
  token: String!
}

type Mutation {
  signup(
    userName: String!
    email: String!
    password: String!
    firstName: String!
    lastName: String!
  ): AuthPayload!

  login(email: String!, password: String!): AuthPayload!
}
